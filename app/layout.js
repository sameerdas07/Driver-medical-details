import './globals.css'

export const metadata = { title: 'Driver Data Hub', description: 'Approved driver medical validity and slip management' }

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>
}